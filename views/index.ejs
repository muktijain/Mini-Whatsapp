<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Whatsapp</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <link 
    rel="stylesheet" 
    href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" 
    />
    <style>
        body{background-color: #ced1d4;}
        .card{background-color: #7f7f81;}
    </style>
</head>
<body class= "text-white">

    <nav class="navbar navbar-tertiary bg-light shadow-lg p-3 mb-5 bg-body-dark rounded-2 m-3 p-2">
    <div class="container-fluid ">
        <a class="navbar-brand"><i class="bi bi-chat-text-fill"></i>  Mini Whatsapp</a>
        <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search"/>
        <button class="btn btn-light" type="submit">Search</button>
        </form>
    </div>
    </nav>
    <br />
    <div class="d-flex justify-content-between align-items-center mb-3">
      <h3 class="text-dark m-3">All Chats</h3>
      <form method="GET" action="/chats/new">
        <button class="btn btn-outline-dark m-3"> <i class="bi bi-plus"> New Chat</i></button>
      </form>
    </div>

    <%for(let chat of chats) { %>
    <div class="card chat-card shadow-lg p-3 m-3 bg-body-dark rounded-3">
        <div class="d-flex justify-content-between align-items-center">
            <p class="mb-1">
                From: <i> <%= chat.from %> </i> 
                &#8594;  
                Receieved By: <i> <%= chat.to %> </i></p>
            <br/>
            <p class="text-light ">
                Send at: <%= chat.created_at.toString().split(" ")[4] %>
                <br/>
                On: <%=chat.created_at.toString().split(" ")[0] %>, <%=chat.created_at.toString().split(" ").slice(1, 4).join("-") %>
            </p>
        </div>
        
        <div class="msg p-3 mt-2 rounded-4">
            <p class="mb-0"><%= chat.msg %></p>

            <hr/>

            <div class="mt-3 d-flex gap-2">
                <form method="GET" action="/chats/<%=chat.id%>/edit">
                    <button class="btn btn-outline-dark">Edit <i class="bi bi-pen-fill"></i></button>
                </form>
                <br />
                <form method="GET" action="/chats/<%=chat.id%>/delete">
                    <button class="btn btn-outline-dark">Delete <i class="bi bi-archive-fill"></i></button>
                </form>
            </div>
        </div>

    </div>
    <br /> <br />
    <%}%>

</body>
</html>





